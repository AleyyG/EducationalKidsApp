<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aleyna.firstgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".AnimalGameActivity"></activity>
        <activity android:name=".MathGameActivity" />
        <activity android:name=".FoodGameActivity" />
        <activity android:name=".PuzzleGameActivity" />
        <activity android:name=".NumberGameActivity" />
        <activity android:name=".ColorGameActivity" />
        <activity android:name=".GameActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>